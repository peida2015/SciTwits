var React = require('react');

var ProjectView = React.createClass({
  render: function () {
    return(
      <div>
        This is ProjectView.
      </div>
    )
  }
});

module.exports = ProjectView;
